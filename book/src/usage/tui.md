# Interactive TUI

Run `adroit` with no subcommand to launch the interactive terminal interface:

```sh
adroit
```

The TUI provides a rich, keyboard-driven interface for browsing and managing your decision log.

> **Note:** The TUI is currently under development. Check back for updates as new features land.
